# Docker Deployment Guide - Frontend Test

Руководство по развертыванию Frontend-test с использованием Docker на Beget.

## 📋 Содержание

- [Предварительные требования](#предварительные-требования)
- [Структура файлов](#структура-файлов)
- [Быстрый старт](#быстрый-старт)
- [Конфигурация](#конфигурация)
- [Деплой](#деплой)
- [Управление контейнером](#управление-контейнером)
- [Troubleshooting](#troubleshooting)

## 🔧 Предварительные требования

- Docker и Docker Compose установлены на сервере
- SSH доступ к серверу Beget
- Node.js 20+ (для локальной разработки)

## 📁 Структура файлов

```
frontend-test/
├── Dockerfile              # Multi-stage Docker образ
├── docker-compose.yml      # Конфигурация сервисов
├── deploy.sh              # Скрипт автоматического деплоя
├── .dockerignore          # Исключения при сборке
└── next.config.ts         # Next.js с output: 'standalone'
```

## 🚀 Быстрый старт

### Локальная разработка

```bash
# Запуск dev сервера
npm run dev

# Открыть http://localhost:4000
```

### Production развертывание

```bash
# Простой деплой одной командой
./deploy.sh
```

## ⚙️ Конфигурация

### Docker Compose

**Порты:**
- Внутренний: `3000` (Next.js в контейнере)
- Внешний: `5001` (доступ с хоста)

**Имя контейнера:** `devmakers-frontend-test`

**Health Check:**
- Проверка каждые 30 секунд
- Timeout: 10 секунд
- Повторы: 3
- Начальная задержка: 40 секунд

### Next.js Configuration

В `next.config.ts` обязательно должно быть:

```typescript
const nextConfig: NextConfig = {
  output: 'standalone', // Для Docker
};
```

## 📦 Деплой

### Автоматический деплой

```bash
cd /var/www/frontend-test
./deploy.sh
```

Скрипт выполнит:
1. ✅ Остановку старого контейнера
2. ✅ Удаление старых образов
3. ✅ Пересборку образа с нуля
4. ✅ Запуск нового контейнера
5. ✅ Проверку здоровья сервиса

### Ручной деплой

```bash
# 1. Остановка контейнеров
docker-compose down

# 2. Пересборка образа
docker-compose build --no-cache

# 3. Запуск контейнеров
docker-compose up -d

# 4. Проверка логов
docker-compose logs -f
```

## 🔍 Управление контейнером

### Просмотр статуса

```bash
# Список запущенных контейнеров
docker ps

# Статус через docker-compose
docker-compose ps

# Проверка здоровья
docker inspect devmakers-frontend-test | grep -A 10 Health
```

### Логи

```bash
# Все логи
docker-compose logs

# Последние логи с отслеживанием
docker-compose logs -f

# Последние 100 строк
docker-compose logs --tail=100

# Логи только frontend-test
docker logs devmakers-frontend-test
```

### Перезапуск

```bash
# Перезапуск контейнера
docker-compose restart

# Остановка
docker-compose stop

# Запуск
docker-compose start
```

### Вход в контейнер

```bash
# Bash в контейнере
docker exec -it devmakers-frontend-test sh

# Проверка файлов
docker exec devmakers-frontend-test ls -la
```

## 🌐 URL доступа

### Production
- **HTTPS:** https://test.devmakers.dev
- **HTTP (локально):** http://localhost:5001

### Development
- http://localhost:4000

## 🔧 Troubleshooting

### Контейнер не запускается

```bash
# Проверка логов
docker-compose logs frontend-test

# Проверка статуса
docker-compose ps

# Проверка здоровья
docker inspect devmakers-frontend-test
```

### Порт занят

```bash
# Найти процесс на порту 5001
lsof -ti:5001

# Убить процесс
kill -9 $(lsof -ti:5001)

# Или остановить другие контейнеры
docker ps -a
docker stop <container_id>
```

### Образ не собирается

```bash
# Очистка Docker кэша
docker system prune -a

# Пересборка без кэша
docker-compose build --no-cache

# Проверка места на диске
df -h
```

### Старый код в production

```bash
# Полная очистка и пересборка
docker-compose down
docker rmi frontend-test_frontend-test
docker-compose build --no-cache
docker-compose up -d
```

### Проблемы с памятью

```bash
# Статистика использования ресурсов
docker stats devmakers-frontend-test

# Увеличить лимит памяти в docker-compose.yml
# (добавить в секцию deploy)
```

## 📊 Мониторинг

### Использование ресурсов

```bash
# Реальное время
docker stats devmakers-frontend-test

# Использование диска
docker system df
```

### Проверка доступности

```bash
# Локальная проверка
curl http://localhost:5001

# Внешняя проверка
curl https://test.devmakers.dev
```

## 🔄 Обновление

```bash
# Pull последних изменений
git pull origin main

# Деплой обновлений
./deploy.sh
```

## 📝 Заметки

1. **Автоматический перезапуск:** Контейнер настроен с `restart: unless-stopped`
2. **Health Check:** Автоматическая проверка здоровья каждые 30 секунд
3. **Standalone Mode:** Next.js собирается в standalone режиме для минимального размера
4. **Multi-stage Build:** Оптимизированная сборка в 3 этапа
5. **Безопасность:** Контейнер работает от непривилегированного пользователя

## 🆘 Поддержка

При возникновении проблем:

1. Проверьте логи: `docker-compose logs -f`
2. Проверьте статус: `docker-compose ps`
3. Проверьте health check: `docker inspect devmakers-frontend-test`
4. Попробуйте полную пересборку: `./deploy.sh`

## 🔗 Полезные ссылки

- [Docker Documentation](https://docs.docker.com/)
- [Next.js Docker Deployment](https://nextjs.org/docs/deployment#docker-image)
- [Docker Compose Reference](https://docs.docker.com/compose/compose-file/)

