## Тестовое задание — 2025‑11‑18

### 🏗️ Структура проекта

- ✅ Проект переведён на архитектуру **FSD** (Feature‑Sliced Design) (см. структуру в файле STRUCTURE.md).
- ✅ Добавлена страница **404** для обработки отсутствующих маршрутов.
- ✅ Реализован `layout` для маршрута `/portfolio/[категория]`: при отсутствии категории отображается страница 404.
- ✅ Статичные данные страниц вынесены в отдельные файлы и **типизированы**.
- ✅ Добавлен `ThemeScrypt` в head для установки темы до того, как React выполнит гидратацию чтобы предотвратить появление неправильной темы
- ✅ В корень проекта помещен оптимальный файл `globals.css`

### 🖼️ Работа с изображениями

- ✅ В компоненте `ImageWithFallback.tsx` тег `<img>` заменён на компонент `<Image>` из `next/image`.
- ✅ Для изображения `portfolio-default.png` были добавлены классы с использованием **Tailwind CSS** на страницах типа
  - `/portfolio/landings`,
  - `/portfolio/corporate`,
  - `/portfolio/landings/[название_проекта]`;
- ✅ Классы адаптированы под разные размеры экрана с использованием **Tailwind CSS**.

### ✩ Анимации

- ✅ Все анимации оптимизированы для **низкопроизводительных устройств**: улучшена плавность, устранены конфликтующие классы Tailwind.
- ✅ Исправлена анимация увеличения чисел в `Stats.tsx`: теперь корректно работает на экранах без боковых полей (включая карточки у левого края).
- ✅ Создан и применён паттерн бесшовной анимации карусели, устранивший эффект рывка после полного цикла анимации:
  - Дублированным элементам присвоены разные классы анимаций (`animate-marquee1`, `animate-marquee2`).
  - Заданы корректные `padding` для бесшовной анимации.
  - Родительскому элементу добавлен класс `animate-marquee` для остановки анимации при `hover`.
- ✅ В `Clients.tsx` бесконечная `motion`-анимация заменена на `animate-marquee`:
  - Установлен `cursor-default` для устранения курсора вида `text` при наведении на элементы карусели.

### ✨ Дополнительные улучшения UI/UX (БОНУС)

- ✅ Созданы и внедрены **стилизованные компоненты** для заголовков и описаний (с `motion` и без):
  - Заменены подходящие теги `h1`, `h2`, `p` на новые компоненты.
- ✅ Добавлен `cursor-pointer` для кнопок:
  - «Оставить заявку» в `Hero.tsx`, `Stats.tsx`, `FeaturesGrid.tsx`, `AIConsultantPreview.tsx`;
  - Смена темы в `Navigation.tsx`;
  - Другие элементы, требующие интерактивного курсора.
- ✅ Оптимизирован размер заголовка в секции `JoinCTA` на странице `/team` (исправлено переполнение на мобильных устройствах).

### 🛠️ Технические правки

- ✅ В `Typography.tsx` комментарии перенесены в верхнюю часть файла для устранения ошибки `char is not defined`.
- ✅ Удалены проблемные комментарии в других файлах, использующих `styled components`.

### 🐞 Найденные баги

- **Проблема с навигационным меню (ПК)**:

  - При медленном наведении курсора снизу вверх на вкладку «Портфолио» курсор меняет тип, открывая выпадающее меню.
  - При дальнейшем движении вверх курсор становится `default`, меню сворачивается, а при возврате к тексту меню не восстанавливается.
  - Наблюдается мигание меню (появление/исчезновение на миллисекунды), ухудшающее UX.

- **Ошибка фона навигационного меню**:
  - При прокрутке вниз и обновлении страницы фон остаётся прозрачным до первого изменения позиции скролла.
  - Проблема наблюдается на всех устройствах.
